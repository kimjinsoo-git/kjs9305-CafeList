<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.macaronagaintoay">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:versionCode="1"
        android:versionName="1.1"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.MacaronAgainToay">
        <activity android:name=".Activity.IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Activity.MainActivity"/>
        <activity android:name=".Activity.MainsActivity"/>
        <activity android:name=".Activity.DetailActivity"/>
        <activity android:name=".Activity.ListCafeActivity"/>
        <activity android:name=".Activity.AdvertisionActivity"/>
        <activity android:name=".Activity.CafeDetailActivity"/>
        <activity android:name=".Activity.ListHomeCafeActivity"/>

        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="sodfjq07ho" />


    </application>

    <uses-permission android:name="android.permission.INTERNET" />              <!--반드시 추가-->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>   <!--위치정보활용시 추가-->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/> <!--위치정보활용시 추가-->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>      <!--WIFI 상태활용시 추가-->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>      <!--WIFI 상태활용시 추가-->



</manifest>